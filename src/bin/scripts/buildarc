#!/bin/csh

# Remove the backups that are more than 10 days old
#
find ~/archives/buildarc -name \* -mtime +10 -print -exec rm -rf {} \;

cd $HOME

setenv TARFILE buildarc-`$RAVEN_ROOT/bin/datestr.pl`.tgz

tar czf $TARFILE builder

chmod 0400 $TARFILE

mv $TARFILE ~/archives/buildarc
